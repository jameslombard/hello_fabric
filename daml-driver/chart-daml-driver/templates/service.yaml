apiVersion: v1
kind: Service
metadata:
  name: {{ include "chart-daml-driver.fullname" . }}
  labels:
    {{- include "chart-daml-driver.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "chart-daml-driver.selectorLabels" . | nindent 4 }}
