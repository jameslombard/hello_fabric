apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "chart-daml-driver.fullname" . }}-test-connection"
  labels:
    {{- include "chart-daml-driver.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "chart-daml-driver.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
